<h1>Tasks</h1>
<% @tasks.each do |task| %>
 <h2><%= link_to task.title, task_path(task) %></h2>
  <p><%= task.details %></p>
  <p><%= task.completed %></p>
  <p><%= link_to  "🧨", task_path(task), method: :delete %></p>
<% end %>
